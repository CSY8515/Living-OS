# Module Manifests

The executable v2.0 Stable manifest catalog is currently declared by `modules/catalog.py`. This directory is reserved for validated serialized manifests when package discovery is introduced. Future roadmap modules are intentionally absent.
